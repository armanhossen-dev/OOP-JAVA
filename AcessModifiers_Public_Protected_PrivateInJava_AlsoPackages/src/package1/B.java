package package1;
import package2.*;

public class B {
	private String privateMessage = "This message is private";
	/// when something is private then it is only visible in that class not by other class or package
	/// now only class B has accesss to this private variable
}
